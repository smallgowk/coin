<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:materialdesign="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <!--<com.viewpagerindicator.TabPageIndicator-->
        <!--android:id="@+id/indicator"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_width="match_parent"-->
        <!--android:background="#ffffffff"-->
        <!--/>-->

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/pagerTabStrip"
        app:pstsShouldExpand="true"
        app:pstsTextAllCaps="false"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:textColor="@color/color_tab"
        android:textSize="14sp"
        app:pstsDividerColor="@color/transparent"
        app:pstsIndicatorColor="@color/white"
        app:pstsIndicatorHeight="2dp"
        app:pstsUnderlineHeight="0dp"
        app:pstsTabBackground="@drawable/drawable_transparent"
        />
    
    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/pagerTabStrip"
        >

    </android.support.v4.view.ViewPager>
    
    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@drawable/drawable_shadow_down"
        android:layout_below="@+id/pagerTabStrip"
        />
    <com.gc.materialdesign.views.ButtonFloat
        android:id="@+id/buttonMyLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="24dp"
        android:background="@drawable/bg_float_button_red"
        materialdesign:iconDrawable="@drawable/ic_add_white_24dp"
        materialdesign:rippleColor="@color/colorPrimaryDark"
        />

</RelativeLayout>